{
  "name": "pauls-pure-functions",
  "version": "0.2.12",
  "description": "A growing library of useful functions for data manipulation in JavaScript. Also abstractions to help with everyday web development, working with URL queries, network requests, promises, etc.",
  "private": false,
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "build": "npm run cjs && npm run dist && rm -rf browser && mv dist browser",
    "dist": "rm -rf .parcel-cache && parcel build ./src/*",
    "cjs": "rollup ./src/*.js --format cjs --dir cjs/ --exports auto"
  },
  "targets": {
    "app": {
      "engines": {
        "browsers": "> 0.75%"
      }
    }
  },
  "type": "module"
}
