{
  "name": "universal-common-functions",
  "version": "0.1.5",
  "description": "An unfinished library of useful functions for data manipulation in JavaScript. Also abstractions to help with everyday web development, working with URL queries, network requests, promises, etc.",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "parcel": "^1.10.3"
  },
  "scripts": {
    "build": "rm -rf .parcel-cache && parcel build cconsole.js cconsoleInit.js",
    "parcel_v2_build": "npm run before_parcel && rm -rf .parcel-cache && parcel build cconsole.js cconsoleInit.js & npm run before_parcel",
    "parcel_v2_before": "sed -i '' 's/\"main\":/\"xmain\":/' package.json && sed -i '' 's/\"module\":/\"xmodule\":/' package.json",
    "parcel_v2_after": "sed -i '' 's/\"xmain\":/\"main\":/' package.json && sed -i '' 's/\"xmodule\":/\"module\":/' package.json"
  },
  "targets": {
    "app": {
      "engines": {
        "browsers": "> 0.75%"
      }
    }
  },
  "type": "module",
  "main": "./src/index.js"
}
